<%= form_with(model: poll, local: true) do |form| %>
  <% if poll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poll.errors.count, "error") %> prohibited this poll from being saved:</h2>

      <ul>
      <% poll.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id, id: :poll_category_id %>
  </div>

  <div class="field">
    <%= form.label :poll_text %>
    <%= form.text_area :poll_text, id: :poll_poll_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
